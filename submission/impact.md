An agent running in a sandbox that has any editable hook script can:

- Run arbitrary code outside the sandbox
- Download arbitrary payloads despite the network sandbox
- Install arbitrary software on the host machine
